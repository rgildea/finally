---
phase: 02-backend-apis
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - backend/app/routers/watchlist.py
  - backend/app/main.py
  - backend/tests/test_watchlist.py
autonomous: true
requirements: [WTCH-01, WTCH-02, WTCH-03, TEST-04]
must_haves:
  truths:
    - "GET /api/watchlist returns the watched tickers each with its latest cached price"
    - "POST /api/watchlist adds a ticker; the polling loop picks it up on its next cycle with no signaling"
    - "DELETE /api/watchlist/{ticker} removes the ticker from the DB and clears it from the price cache"
  artifacts:
    - path: "backend/app/routers/watchlist.py"
      provides: "Watchlist GET/POST/DELETE endpoints and WatchlistRequest model"
      contains: "watchlist"
    - path: "backend/tests/test_watchlist.py"
      provides: "Unit tests for watchlist CRUD response shapes and persistence"
      contains: "test_add_ticker"
  key_links:
    - from: "backend/app/routers/watchlist.py"
      to: "watchlist table"
      via: "INSERT OR IGNORE / DELETE inside with con:"
      pattern: "(INSERT OR IGNORE INTO watchlist|DELETE FROM watchlist)"
    - from: "backend/app/routers/watchlist.py"
      to: "app.market.cache.price_cache"
      via: "get_many for GET prices, remove on DELETE"
      pattern: "price_cache\\.(get_many|remove)\\("
    - from: "backend/app/main.py"
      to: "app.routers.watchlist.router"
      via: "app.include_router(watchlist_router)"
      pattern: "include_router\\(watchlist_router\\)"
---

<objective>
Build watchlist CRUD: list watched tickers with live prices, add a ticker, remove a ticker.

Purpose: Completes the Phase 2 REST surface. The watchlist is the source of truth the polling loop reads every cycle, so adds/removes take effect automatically with no extra signaling. This closes the last Phase 2 requirements (WTCH-*) and the API-route response-shape coverage (TEST-04).
Output: `backend/app/routers/watchlist.py` (GET/POST/DELETE + WatchlistRequest), watchlist router registered in `main.py`, and `backend/tests/test_watchlist.py`.
</objective>

<execution_context>
@/Users/ryan/projects/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryan/projects/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-apis/02-RESEARCH.md
@planning/PLAN.md
@.planning/phases/02-backend-apis/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Watchlist GET/POST/DELETE endpoints and WatchlistRequest</name>
  <files>backend/app/routers/watchlist.py, backend/tests/test_watchlist.py</files>
  <read_first>
    - backend/app/db/database.py — get_connection() and get_watchlist_tickers() (re-queries DB each call; this is why no signaling is needed)
    - backend/app/db/schema.py — watchlist columns (id, user_id, ticker, added_at) with UNIQUE(user_id, ticker)
    - backend/app/db/seed.py — id/timestamp convention: str(uuid.uuid4()) and datetime.now(timezone.utc).isoformat()
    - backend/app/market/cache.py — async get_many(tickers) returns dict; async remove(ticker)
    - backend/app/routers/portfolio.py — established router/validator/connection conventions from plan 02-02 to mirror
    - backend/app/main.py — router registration site (health, market, portfolio already included)
    - .planning/phases/02-backend-apis/02-RESEARCH.md — Pattern 6 (watchlist needs no signaling; DELETE also clears cache), Pitfall 6 (ticker.upper())
  </read_first>
  <behavior>
    - Test 1 (test_get_watchlist): GET /api/watchlist on a fresh seeded DB returns 200 with 10 entries; each entry has ticker and price (price may be null when cache empty)
    - Test 2 (test_get_watchlist_with_prices): with a mocked cache, returned entries carry the cached price for tickers present in cache
    - Test 3 (test_add_ticker): POST /api/watchlist {ticker:"PYPL"} returns 200 and a subsequent get_watchlist_tickers() includes "PYPL"
    - Test 4 (test_add_ticker_lowercase_normalized): POST {ticker:"pypl"} stores "PYPL" (uppercased)
    - Test 5 (test_add_duplicate_idempotent): POST an already-watched ticker does not create a duplicate row (UNIQUE constraint / INSERT OR IGNORE)
    - Test 6 (test_remove_ticker): DELETE /api/watchlist/AAPL returns 200 and get_watchlist_tickers() no longer includes "AAPL"; price_cache.remove was called for "AAPL"
  </behavior>
  <action>
    Create backend/app/routers/watchlist.py. Imports: uuid, datetime/timezone; `from fastapi import APIRouter`; `from pydantic import BaseModel, field_validator`; `from app.db.database import get_connection`; `from app.market.cache import price_cache`. Define `router = APIRouter(prefix="/api", tags=["watchlist"])`.

    Define WatchlistRequest(BaseModel) with field ticker: str and a field_validator that uppercases+strips and raises ValueError if empty or len > 10 (mirror TradeRequest.normalize_ticker from portfolio.py so 422 is returned for bad input).

    `@router.get("/watchlist")` `async def get_watchlist()`: open get_connection() in try/finally, SELECT ticker FROM watchlist WHERE user_id='default' ORDER BY added_at; close; then `prices = await price_cache.get_many([t for t in tickers])`. Return a list of {"ticker": t, "price": round(prices[t].price, 4) if t in prices else None} (async because it awaits the cache).

    `@router.post("/watchlist")` `def add_ticker(req: WatchlistRequest)`: open get_connection() in try/finally; inside `with con:` execute INSERT OR IGNORE INTO watchlist (id, user_id, ticker, added_at) VALUES (str(uuid.uuid4()), 'default', req.ticker, now-iso). Return {"status":"ok","ticker":req.ticker}. No signaling needed — polling loop re-queries (Pattern 6).

    `@router.delete("/watchlist/{ticker}")` `async def remove_ticker(ticker: str)`: normalize `ticker = ticker.upper().strip()`; open get_connection() in try/finally; inside `with con:` execute DELETE FROM watchlist WHERE user_id='default' AND ticker=?; close; then `await price_cache.remove(ticker)` to clear stale cache (Pattern 6). Return {"status":"ok","ticker":ticker}. async because it awaits the cache.

    Create backend/tests/test_watchlist.py mirroring test_app.py fixtures (tmp_db monkeypatch DB_PATH; init_db() before each test). Use httpx.AsyncClient(ASGITransport) for endpoint tests; patch price_cache.get_many / remove with AsyncMock where needed. Cover behaviors 1-6.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_watchlist.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/test_watchlist.py -x -q` passes with at least 6 tests
    - `grep -c "INSERT OR IGNORE INTO watchlist" backend/app/routers/watchlist.py` returns 1
    - `grep -c "DELETE FROM watchlist" backend/app/routers/watchlist.py` returns 1
    - `grep -c "price_cache.remove" backend/app/routers/watchlist.py` returns 1
    - `grep -c "async def get_watchlist" backend/app/routers/watchlist.py` returns 1
    - Lowercase-add test proves "pypl" is stored as "PYPL"; duplicate-add test proves no duplicate row
  </acceptance_criteria>
  <done>Watchlist GET (with cached prices), POST (uppercased, idempotent add), and DELETE (DB remove + cache clear) work; WatchlistRequest validates input; tests cover all three operations and edge cases.</done>
</task>

<task type="auto">
  <name>Task 2: Register watchlist router in main.py</name>
  <files>backend/app/main.py, backend/tests/test_watchlist.py</files>
  <read_first>
    - backend/app/main.py — current router includes (health, market, portfolio) and import block; add the watchlist import + include without disturbing existing lines or the lifespan
    - backend/tests/test_watchlist.py — the endpoint tests from Task 1 exercise the registered router
  </read_first>
  <action>
    In backend/app/main.py add `from app.routers.watchlist import router as watchlist_router  # noqa: E402` alongside the other router imports, and `app.include_router(watchlist_router)` after the existing portfolio router include. Add one route-level test in test_watchlist.py (test_watchlist_routes_registered) that GETs /api/watchlist via httpx.AsyncClient(ASGITransport) and asserts status 200, confirming the router is mounted on the app.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_watchlist.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "include_router(watchlist_router)" backend/app/main.py` returns 1
    - `cd backend && uv run pytest tests/test_watchlist.py::test_watchlist_routes_registered -x` passes
    - `cd backend && uv run pytest -q` — full suite green
  </acceptance_criteria>
  <done>Watchlist router is mounted on the FastAPI app and reachable at /api/watchlist; full backend test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /api/watchlist, DELETE /api/watchlist/{ticker} | Untrusted ticker input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | POST /api/watchlist body | mitigate | WatchlistRequest validator: uppercase, non-empty, len <= 10 → 422 before DB write |
| T-02-08 | Tampering | DELETE path param ticker | mitigate | Parameterized DELETE query; ticker.upper().strip() normalization; no SQL string interpolation |
| T-02-09 | Information Disclosure | GET /api/watchlist | accept | Returns only public ticker symbols and market prices; no PII (single-user, no auth by design) |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH.md Package Legitimacy Audit: zero new packages) |
</threat_model>

<verification>
- `cd backend && uv run pytest -q` — full suite green (all Phase 2 routers + tests)
- Watchlist add/remove reflected by get_watchlist_tickers() (which the polling loop calls each cycle — no manual signaling)
</verification>

<success_criteria>
- GET /api/watchlist returns tickers with latest cached prices (WTCH-01)
- POST /api/watchlist adds a ticker, idempotently, uppercased; polling loop picks it up next cycle (WTCH-02)
- DELETE /api/watchlist/{ticker} removes from DB and clears cache (WTCH-03)
- Watchlist route response shapes/status codes covered by tests (TEST-04)
- Full backend test suite green
</success_criteria>

<output>
Create `.planning/phases/02-backend-apis/02-03-SUMMARY.md` when done
</output>
