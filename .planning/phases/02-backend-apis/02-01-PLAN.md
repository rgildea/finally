---
phase: 02-backend-apis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routers/market.py
  - backend/app/db/database.py
  - backend/app/main.py
  - backend/tests/test_market_sse.py
autonomous: true
requirements: [STRM-01, STRM-02, STRM-03]
must_haves:
  truths:
    - "GET /api/stream/prices streams SSE price_update events at ~500ms cadence"
    - "Each SSE event data contains ticker, price, prev_price, change_pct, and timestamp"
    - "The SSE generator reads exclusively from the price_cache singleton, never the data source"
    - "get_connection() sets busy_timeout=5000 so concurrent writers retry instead of erroring"
  artifacts:
    - path: "backend/app/routers/market.py"
      provides: "SSE /api/stream/prices endpoint via EventSourceResponse"
      contains: "stream/prices"
    - path: "backend/tests/test_market_sse.py"
      provides: "Unit tests for SSE generator content, fields, and cache-source"
      contains: "test_sse"
  key_links:
    - from: "backend/app/routers/market.py"
      to: "app.market.cache.price_cache"
      via: "await price_cache.get_all() inside the SSE generator loop"
      pattern: "price_cache\\.get_all"
    - from: "backend/app/main.py"
      to: "app.routers.market.router"
      via: "app.include_router(market_router)"
      pattern: "include_router\\(market_router\\)"
---

<objective>
Build the SSE price-streaming endpoint `GET /api/stream/prices` and add the SQLite `busy_timeout` pragma that all later write-path plans (trades, watchlist) depend on.

Purpose: Streaming live prices is the heartbeat of the trading terminal — the frontend's EventSource connects here. The busy_timeout addition is foundational for the concurrent SQLite writes introduced in plan 02 and 03, so it lands here in wave 1.
Output: `backend/app/routers/market.py` (SSE router), `busy_timeout` added to `get_connection()`, market router registered in `main.py`, and `backend/tests/test_market_sse.py`.
</objective>

<execution_context>
@/Users/ryan/projects/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryan/projects/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-apis/02-RESEARCH.md
@planning/PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add busy_timeout pragma to get_connection()</name>
  <files>backend/app/db/database.py, backend/tests/test_db.py</files>
  <read_first>
    - backend/app/db/database.py — current get_connection() sets WAL and foreign_keys pragmas; you are adding one more line
    - backend/tests/test_db.py — existing DB test patterns; tmp_db fixture monkeypatches DB_PATH
    - .planning/phases/02-backend-apis/02-RESEARCH.md — Pitfall 2 (SQLite database is locked); busy_timeout=5000 recommended
  </read_first>
  <behavior>
    - Test 1: a connection returned by get_connection() reports `PRAGMA busy_timeout` == 5000
    - Test 2: existing get_connection() pragmas (journal_mode=WAL, foreign_keys=ON) remain unchanged
  </behavior>
  <action>
    In backend/app/db/database.py, inside get_connection(), after the existing `con.execute("PRAGMA foreign_keys=ON")` line, add `con.execute("PRAGMA busy_timeout=5000")`. Do not change any other line. Add a test to backend/tests/test_db.py named test_busy_timeout_set that opens a connection via get_connection() and asserts `con.execute("PRAGMA busy_timeout").fetchone()[0] == 5000`.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_db.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/test_db.py::test_busy_timeout_set -x` passes
    - `grep -c "PRAGMA busy_timeout=5000" backend/app/db/database.py` returns 1
    - All 7 test_db.py tests pass (6 existing + 1 new)
  </acceptance_criteria>
  <done>get_connection() sets busy_timeout=5000 and a test proves it; existing WAL and foreign_keys pragmas untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SSE /api/stream/prices endpoint and registration</name>
  <files>backend/app/routers/market.py, backend/app/main.py, backend/tests/test_market_sse.py</files>
  <read_first>
    - backend/app/market/cache.py — price_cache singleton; async get_all() returns dict[str, PriceUpdate]
    - backend/app/market/interface.py — PriceUpdate fields (ticker, price, prev_price, timestamp) plus computed change_pct; model_dump() includes change_pct
    - backend/app/routers/health.py — existing router pattern: APIRouter(prefix="/api")
    - backend/app/main.py — where routers are registered via app.include_router(...)
    - backend/tests/test_app.py — tmp_db fixture and httpx.AsyncClient(ASGITransport) test pattern
    - .planning/phases/02-backend-apis/02-RESEARCH.md — Pattern 1 (SSE endpoint), Pitfall 1 (SSE test hangs — test the generator directly via __anext__)
  </read_first>
  <behavior>
    - Test 1 (test_sse_event_fields): after price_cache.update("AAPL", a PriceUpdate), calling the generator and awaiting its first event yields a ServerSentEvent whose data dict contains keys ticker, price, prev_price, change_pct, timestamp
    - Test 2 (test_sse_event_value): the yielded event data["ticker"] == "AAPL" and data["price"] matches the cached price
    - Test 3 (test_sse_reads_cache): with an empty price_cache, the generator yields no per-ticker events in its first cycle (proving it reads the cache, not the data source) — assert it sleeps/awaits without raising
  </behavior>
  <action>
    Create backend/app/routers/market.py with `from fastapi import APIRouter`, `from fastapi.sse import EventSourceResponse, ServerSentEvent`, and `from app.market.cache import price_cache`. Define `router = APIRouter(prefix="/api", tags=["market"])`. Define an async generator function and a route `@router.get("/stream/prices", response_class=EventSourceResponse)` returning it. The generator loops forever: `updates = await price_cache.get_all()`, then for each ticker, update in updates.items() yield `ServerSentEvent(data=update.model_dump(), event="price_update")`, then `await asyncio.sleep(0.5)`. model_dump() includes change_pct because it is a computed_field on PriceUpdate. Do NOT use manual `data: ...\n\n` formatting. To make the generator unit-testable, expose it as a named module-level async function (e.g. `async def price_event_stream()`) that the route returns, so tests can call it directly and use `.__anext__()` then `.aclose()`.

    In backend/app/main.py add `from app.routers.market import router as market_router  # noqa: E402` alongside the existing health import, and `app.include_router(market_router)` after the existing `app.include_router(health_router)` line.

    Create backend/tests/test_market_sse.py following the test_app.py import and fixture conventions. Use a fixture that clears price_cache before each test (await price_cache.remove on known tickers, or reset its _data). For field tests, await price_cache.update("AAPL", PriceUpdate(ticker="AAPL", price=190.0, prev_price=189.0, timestamp="2024-01-15T14:30:00.000Z")), then `gen = price_event_stream()`, `event = await gen.__anext__()`, assert on event.data keys, then `await gen.aclose()`. Do NOT drive the generator via httpx streaming — it never terminates (research Pitfall 1).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_market_sse.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/test_market_sse.py -x -q` passes with at least 3 tests
    - `grep -c "from fastapi.sse import" backend/app/routers/market.py` returns 1
    - `grep -c "price_cache.get_all" backend/app/routers/market.py` returns 1
    - `grep -c "include_router(market_router)" backend/app/main.py` returns 1
    - Test for event fields asserts presence of all five keys: ticker, price, prev_price, change_pct, timestamp
    - No occurrence of the manual SSE string `data:` formatting in market.py: `grep -c 'data: ' backend/app/routers/market.py` returns 0
  </acceptance_criteria>
  <done>GET /api/stream/prices is registered and streams price_update SSE events sourced from price_cache; generator-level unit tests prove event fields and cache-only sourcing; full suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → SSE endpoint | Client opens a long-lived GET; no request body, read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | /api/stream/prices | accept | Single-user app, no auth by design; only public market prices are streamed; no PII |
| T-02-02 | Denial of Service | SSE generator loop | mitigate | Reads from in-memory cache only (no DB/network per cycle); fixed 0.5s cadence bounds work per connection |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan (RESEARCH.md Package Legitimacy Audit: zero new packages); fastapi.sse import verified against installed fastapi 0.136.3 |
</threat_model>

<verification>
- `cd backend && uv run pytest -q` — full suite green (68 existing + new SSE + busy_timeout tests)
- `cd backend && uv run python -c "from fastapi.sse import EventSourceResponse, ServerSentEvent; print('ok')"` — confirms SSE import works on installed fastapi
</verification>

<success_criteria>
- GET /api/stream/prices registered and yields price_update events with ticker, price, prev_price, change_pct, timestamp (STRM-01, STRM-02)
- SSE generator reads only from price_cache.get_all() (STRM-03)
- get_connection() sets busy_timeout=5000
- Full backend test suite green
</success_criteria>

<output>
Create `.planning/phases/02-backend-apis/02-01-SUMMARY.md` when done
</output>
